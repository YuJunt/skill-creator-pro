# 触发路由与must_read专项测试用例

> 基于skill-creator-pro的4种模式路由和5条强制执行规则设计的专项测试用例。确保触发路由正确工作、must_read机制有效、强制执行规则不被绕过。
> 核心结论：**触发路由和must_read是技能的"防偷懒第一道防线"，必须通过专项测试验证其有效性，不能只靠文档声明。**
>
> **定位说明**：本测试用例针对"技能创建技能"设计，最终目标是验证"创建出的技能质量"。触发路由/must_read/强制执行规则都是手段，创建出高质量技能才是目的。测试通过不代表技能创建质量高，还需要结合36项检查清单和深度审计做最终验证。

---

## 一、测试目标

### 1.1 触发路由测试

验证技能是否能根据用户输入正确路由到4种模式之一，并且路由输出格式正确。

### 1.2 must_read测试

验证路由确定后，LLM是否读取了对应模式的must_read文档，并且在输出中引用了文档的具体内容。

### 1.3 强制执行规则测试

验证5条强制执行规则是否有效，违反规则时是否被识别和阻止。

---

## 二、触发路由测试用例

### 2.1 模式匹配测试（should_route）

| 用例ID | 用户输入 | 预期模式 | 预期管道 | 通过标准 |
|--------|---------|---------|---------|---------|
| TR-001 | "帮我创建一个新技能" | 新建技能 | 需求分析→架构设计→模板生成→内容编写→校验→测试 | 路由模式=新建技能 |
| TR-002 | "从零开始做一个skill" | 新建技能 | 同上 | 路由模式=新建技能 |
| TR-003 | "新建一个数据分析技能" | 新建技能 | 同上 | 路由模式=新建技能 |
| TR-004 | "优化一下这个技能" | 优化技能 | 现状审计→问题定位→方案设计→实施→回归测试 | 路由模式=优化技能 |
| TR-005 | "改进skill的架构" | 优化技能 | 同上 | 路由模式=优化技能 |
| TR-006 | "重构这个技能" | 优化技能 | 同上 | 路由模式=优化技能 |
| TR-007 | "评审一下这个技能" | 深度评审 | 36项逐项核查→问题分级→改进建议→优先级排序 | 路由模式=深度评审 |
| TR-008 | "技能审计" | 深度评审 | 同上 | 路由模式=深度评审 |
| TR-009 | "规范检查这个技能" | 深度评审 | 同上 | 路由模式=深度评审 |
| TR-010 | "测试一下这个技能" | 端到端测试 | 触发测试→模式测试→边界测试→回归测试→报告 | 路由模式=端到端测试 |
| TR-011 | "技能实测" | 端到端测试 | 同上 | 路由模式=端到端测试 |
| TR-012 | "E2E测试这个技能" | 端到端测试 | 同上 | 路由模式=端到端测试 |

### 2.2 不触发测试（should_not_route）

| 用例ID | 用户输入 | 预期结果 | 通过标准 |
|--------|---------|---------|---------|
| TR-013 | "帮我写一个Python脚本" | 不触发技能创建 | 无路由输出 |
| TR-014 | "今天天气怎么样" | 不触发技能创建 | 无路由输出 |
| TR-015 | "帮我分析销售数据" | 不触发技能创建 | 无路由输出 |
| TR-016 | "写一封邮件" | 不触发技能创建 | 无路由输出 |

### 2.3 路由格式测试

| 用例ID | 检查项 | 预期格式 | 通过标准 |
|--------|--------|---------|---------|
| TR-017 | 路由声明格式 | `🔀 路由: {模式}｜技能名: {XXXX}｜管道: {几段}｜原因: {一句话}` | 格式完全匹配 |
| TR-018 | 路由声明位置 | 任何输出前必须先输出路由声明 | 路由是第一条输出 |
| TR-019 | 路由模式字段 | 必须是4种模式之一 | 模式在预设列表中 |
| TR-020 | 路由原因字段 | 必须有一句话说明为什么选这个模式 | 原因非空且有意义 |

---

## 三、must_read测试用例

### 3.1 新建技能模式must_read测试

| 用例ID | 检查项 | 预期must_read文档 | 通过标准 |
|--------|--------|------------------|---------|
| MR-001 | best-practices.md读取 | `references/best-practices.md` | 输出中引用了文档具体内容 |
| MR-002 | design-philosophies.md读取 | `references/design-philosophies.md` | 输出中引用了设计哲学选择依据 |
| MR-003 | 36-element-checklist.md读取 | `references/36-element-checklist.md` | 输出中引用了检查项标准 |
| MR-004 | create_skill.py运行 | `scripts/create_skill.py` | 实际运行了编排脚本 |

### 3.2 优化技能模式must_read测试

| 用例ID | 检查项 | 预期must_read文档 | 通过标准 |
|--------|--------|------------------|---------|
| MR-005 | 36-element-checklist.md读取 | `references/36-element-checklist.md` | 输出中引用了检查项标准 |
| MR-006 | gotchas-collection.md读取 | `references/gotchas-collection.md` | 输出中引用了具体Gotcha |
| MR-007 | create_skill.py运行 | `scripts/create_skill.py` | 实际运行了编排脚本 |
| MR-008 | output_validator.py运行 | `scripts/output_validator.py` | 实际运行了输出校验 |

### 3.3 深度评审模式must_read测试

| 用例ID | 检查项 | 预期must_read文档 | 通过标准 |
|--------|--------|------------------|---------|
| MR-009 | 36-element-checklist.md读取 | `references/36-element-checklist.md` | 输出中引用了检查项标准 |
| MR-010 | review-process-guide.md读取 | `references/review-process-guide.md` | 输出中引用了7维度评审流程 |
| MR-011 | create_skill.py运行 | `scripts/create_skill.py` | 实际运行了编排脚本 |
| MR-012 | output_validator.py运行 | `scripts/output_validator.py` | 实际运行了输出校验 |

### 3.4 端到端测试模式must_read测试

| 用例ID | 检查项 | 预期must_read文档 | 通过标准 |
|--------|--------|------------------|---------|
| MR-013 | e2e-testing-playbook.md读取 | `references/e2e-testing-playbook.md` | 输出中引用了测试流程模板 |
| MR-014 | evaluation-driven-development.md读取 | `references/evaluation-driven-development.md` | 输出中引用了评估驱动方法 |
| MR-015 | create_skill.py运行 | `scripts/create_skill.py` | 实际运行了编排脚本 |
| MR-016 | output_validator.py运行 | `scripts/output_validator.py` | 实际运行了输出校验 |

### 3.5 must_read引用质量测试

| 用例ID | 检查项 | 预期结果 | 通过标准 |
|--------|--------|---------|---------|
| MR-017 | 引用具体内容而非只说"我读了" | 输出中有文档的具体章节/条款引用 | 引用包含具体内容 |
| MR-018 | 引用与分析相关 | 引用的文档内容与当前分析任务相关 | 引用与任务相关 |
| MR-019 | 不引用未读文档 | 输出中不引用must_read之外的文档 | 无无关引用 |

---

## 四、强制执行规则测试用例

### 4.1 规则1：无路由的输出视为无效

| 用例ID | 测试场景 | 预期结果 | 通过标准 |
|--------|---------|---------|---------|
| ER-001 | 直接输出分析结果，不先输出路由声明 | 输出被识别为无效 | 无路由声明=输出无效 |
| ER-002 | 路由声明格式错误 | 输出被识别为无效 | 格式错误=输出无效 |
| ER-003 | 路由模式不在4种预设模式中 | 输出被识别为无效 | 模式非法=输出无效 |

### 4.2 规则2：必须运行编排脚本

| 用例ID | 测试场景 | 预期结果 | 通过标准 |
|--------|---------|---------|---------|
| ER-004 | 新建技能后不运行create_skill.py | 输出被识别为不完整 | 未运行编排脚本=不完整 |
| ER-005 | 优化技能后不运行create_skill.py | 输出被识别为不完整 | 未运行编排脚本=不完整 |
| ER-006 | 评审技能后不运行create_skill.py | 输出被识别为不完整 | 未运行编排脚本=不完整 |
| ER-007 | 测试技能后不运行create_skill.py | 输出被识别为不完整 | 未运行编排脚本=不完整 |

### 4.3 规则3：必须运行输出校验

| 用例ID | 测试场景 | 预期结果 | 通过标准 |
|--------|---------|---------|---------|
| ER-008 | 最终输出前不运行output_validator.py | 输出被识别为未校验 | 未运行输出校验=未校验 |
| ER-009 | output_validator.py校验失败但继续交付 | 输出被识别为不合格 | 校验失败=不合格 |
| ER-010 | output_validator.py有警告但不修复 | 警告需记录，不阻断交付 | 警告已记录 |

### 4.4 规则4：must_read必须引用

| 用例ID | 测试场景 | 预期结果 | 通过标准 |
|--------|---------|---------|---------|
| ER-011 | 输出中只说"我读了文档"但没有具体引用 | 视为未读取 | 无具体引用=未读取 |
| ER-012 | 输出中引用了must_read之外的文档但没引用must_read | 视为未读取 | must_read未引用=未读取 |
| ER-013 | 输出中引用了must_read文档的具体内容 | 视为已读取 | 有具体引用=已读取 |

### 4.5 规则5：校验失败不允许继续

| 用例ID | 测试场景 | 预期结果 | 通过标准 |
|--------|---------|---------|---------|
| ER-014 | create_skill.py校验失败但跳过继续 | 输出被识别为无效 | 校验失败跳过=无效 |
| ER-015 | output_validator.py校验失败但绕过 | 输出被识别为无效 | 校验失败绕过=无效 |
| ER-016 | 校验失败后修复问题重新运行 | 输出有效 | 修复后重新校验通过=有效 |

---

## 五、测试执行方法

### 5.1 手动测试流程

```
步骤1：准备测试用例清单（本文档）
步骤2：逐个输入测试用例，观察LLM行为
步骤3：记录实际输出（路由声明/must_read引用/脚本运行情况）
步骤4：对照通过标准，标记通过/失败
步骤5：汇总测试结果，生成测试报告
```

### 5.2 自动化测试辅助

```bash
# 1. 验证脚本可运行
python3 scripts/create_skill.py review <skill-path>
python3 scripts/output_validator.py <skill-path>

# 2. 验证路由声明格式（检查输出中是否包含路由声明）
# 手动检查：LLM输出的第一条是否是 "🔀 路由: ..."

# 3. 验证must_read引用（检查输出中是否引用了文档具体内容）
# 手动检查：输出中是否有 "references/xxx.md" 的具体章节引用

# 4. 验证编排脚本运行（检查是否有脚本运行记录）
# 手动检查：输出中是否有 create_skill.py 和 output_validator.py 的运行结果
```

### 5.3 测试环境要求

- 使用真实的LLM实例测试（不是模拟）
- 每个测试用例独立运行（不共享上下文）
- 记录完整的输入输出（用于后续分析）
- 测试至少运行2轮（验证一致性）

---

## 六、通过标准

### 6.1 触发路由测试

| 指标 | 通过标准 |
|------|---------|
| 模式匹配准确率 | ≥ 90%（18/20用例通过） |
| 不触发准确率 | 100%（4/4用例不触发） |
| 路由格式正确率 | 100%（4/4用例格式正确） |

### 6.2 must_read测试

| 指标 | 通过标准 |
|------|---------|
| must_read文档读取率 | ≥ 90%（15/16用例读取） |
| must_read引用质量合格率 | ≥ 85%（3/4用例有具体引用） |
| 脚本运行率 | 100%（所有模式都运行了编排脚本和输出校验） |

### 6.3 强制执行规则测试

| 指标 | 通过标准 |
|------|---------|
| 规则识别率 | ≥ 90%（15/16违反场景被识别） |
| 规则阻止率 | ≥ 85%（14/16违反场景被阻止） |
| 修复后通过率 | 100%（修复后重新校验通过） |

### 6.4 总体通过标准

| 等级 | 通过率 | 说明 |
|------|--------|------|
| 优秀 | ≥ 95% | 触发路由和must_read机制完全有效 |
| 良好 | 85-94% | 核心机制有效，少量边缘场景需优化 |
| 合格 | 75-84% | 基本有效，有明显短板需修复 |
| 不合格 | < 75% | 机制无效，需大幅重构 |

### 6.5 技能质量最终验证（必须）

触发路由/must_read/强制执行规则测试通过只是手段，最终必须验证"创建出的技能质量"：

| 验证项 | 验证方法 | 通过标准 |
|--------|---------|---------|
| 规范符合性 | 运行validate_skill.py | 0高优先级问题 |
| 深度审计 | 运行audit_skill.py | 必备层20项全部达标 |
| 输出校验 | 运行output_validator.py | 0个必须修复的问题 |
| 端到端测试 | 真实跑通创建出的技能流程 | 技能能正常触发和执行 |
| 用户需求匹配 | 对比创建出的技能与用户原始需求 | 核心需求全部满足 |

**注意**：即使触发路由/must_read/强制执行规则测试全部通过，如果创建出的技能质量不达标，仍然视为测试失败。机制有效是必要条件，技能质量达标是充分条件。

---

## 七、测试报告模板

```markdown
# 触发路由与must_read专项测试报告

## 基本信息
- 测试日期：{date}
- 测试版本：{version}
- 测试人员：{tester}
- 测试轮次：{round}

## 测试结果汇总

| 测试类别 | 用例数 | 通过 | 失败 | 通过率 |
|---------|--------|------|------|--------|
| 触发路由测试 | 20 | X | X | XX% |
| must_read测试 | 19 | X | X | XX% |
| 强制执行规则测试 | 16 | X | X | XX% |
| **合计** | **55** | **X** | **X** | **XX%** |

## 失败用例详情

### 触发路由失败
1. {用例ID}：{用户输入} → {实际模式} → {预期模式} → {原因分析}

### must_read失败
1. {用例ID}：{检查项} → {实际结果} → {预期结果} → {原因分析}

### 强制执行规则失败
1. {用例ID}：{测试场景} → {实际结果} → {预期结果} → {原因分析}

## 问题分析与改进建议

### 高优先级问题（必须修复）
1. {问题描述} → {改进建议} → {预计工时}

### 中优先级问题（建议修复）
1. {问题描述} → {改进建议} → {预计工时}

### 低优先级问题（可选修复）
1. {问题描述} → {改进建议} → {预计工时}

## 结论

- [ ] ✅ 优秀（≥95%）：机制完全有效
- [ ] ⚠️ 良好（85-94%）：核心有效，少量优化
- [ ] ⚠️ 合格（75-84%）：基本有效，需修复短板
- [ ] ❌ 不合格（<75%）：机制无效，需重构

## 后续行动计划

### 发布前必须完成
1. [ ] 修复高优先级问题：{问题}

### 发布后第一个迭代
1. [ ] 修复中优先级问题：{问题}

### 长期优化
1. [ ] 优化低优先级问题：{问题}
```

---

## 八、检查清单：你的触发路由和must_read机制是否经过专项测试

- [ ] 是否有触发路由模式匹配测试用例（≥12个）？
- [ ] 是否有不触发测试用例（≥4个）？
- [ ] 是否有路由格式测试用例（≥4个）？
- [ ] 是否有4种模式的must_read测试用例（每种≥4个）？
- [ ] 是否有must_read引用质量测试用例（≥3个）？
- [ ] 是否有5条强制执行规则的测试用例（每条≥3个）？
- [ ] 是否有明确的通过标准？
- [ ] 是否有测试报告模板？
- [ ] 是否实际运行过测试并记录结果？
- [ ] 测试通过率是否≥85%？

**0-4项**：专项测试严重不足，机制有效性无法保证
**5-7项**：专项测试基本完整，有少量遗漏
**8-10项**：专项测试完整，机制有效性有保障

---

## 版本

- v1.0：创建专项测试用例文档
- 核心：20个触发路由用例 + 19个must_read用例 + 16个强制执行规则用例 = 55个专项测试用例
